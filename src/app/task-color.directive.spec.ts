import { TaskColorDirective } from './task-color.directive';

describe('TaskColorDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskColorDirective();
    expect(directive).toBeTruthy();
  });
});
